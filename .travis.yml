# ref: https://docs.travis-ci.com/user/languages/python
language: python
python:
  - "3.6"
install:
  - "pip install -r requirements.txt"
  - "pip install -r test-requirements.txt"
script: nosetests --with-xunit --with-coverage --cover-erase --cover-package=adapters --cover-package=esm --cover-min-percentage=70
